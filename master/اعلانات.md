- [ارسال پیامک](#menu)
- [دریافت وضعیت پیامک ارسال شده](#menu)
- [دریافت لیست پیام های ارسالی](#menu)
- [ارسال ایمیل](#menu)
- [دریافت لیست ایمیل های ارسالی](#menu)
- [ارسال push notification](#menu)
- [دریافت لیست push notification](#menu)
- [دریافت وضعیت push notification](#menu)
- [لینک‌های مرتبط](#menu)

## ارسال پیامک
با استفاده از این سرویس امکان ارسال پیامک به یک یا گروهی از شماره(های) امکان­پذیر است.

<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
            var output = new ResultSrv<SmsSrv>();
            var smsVo = SmsVo.ConcreteBuilder
                                .SetToken("{Put your Token}")
                                .SetContent(SmsContentVo.ConcreteBuilder
                                    .SetContent("{Put your Content}")
                                    .SetMobileNumbers({Put your MobileNumbers})
                                    //.SetScheduler("{Put Your Schedular}")
                                    .Build())
                                //.SetServiceName("{Put your ServiceName}")
                                .Build();
            notificationService.SendSms(smsVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
  BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();
        String[] mobileNumber = new String[]{{mobileNumber}};

        SmsContentVo smsContentVo = null;
        try {
            smsContentVo = new SmsContentVo.Builder()
                    .setContent({content})
                    .setMobileNumbers(mobileNumber)
          //          .setScheduler("")
                    .build();
        } catch (PodException e) {
        }
        BodySendSms bodySendSms = null;
        try {
            bodySendSms = new BodySendSms.Builder()
                    .setContent(smsContentVo)
                    .build();
        } catch (com.fanap.podBaseService.exception.PodException e) {
            e.printStackTrace();
        }
        try {
            SendSmsVo sendSmsVo = new SendSmsVo.Builder(baseInfoVo)
                    .setBodySendSms(bodySendSms)
                    .build();
            podNotification.sendSms(sendSmsVo, new OnGetResponseListener<SmsSrv>() {
                @Override
                public void onResponse(ResultVo<SmsSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().getMessageId());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());
        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ===================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "content" => [
            ## ======== Required Parameters ==============
            "content" => "Hello World!",
            "mobileNumbers" => ["mobileNumber1", "mobileNumber2"], # sms receptors
            ## ======== Optional Parameters  =============
//            "scheduler" => "", # send message in particular date time yyyy-MM-dd HH:mm
        ],
        ## ============================ Optional Parameters  ==================================
//        "serviceName" => ""
    ];
    try {
        $result = $NotificationService->sendSMS($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let sendSMSData = {
  // ------ REQUIRED ------
  scApiKey: 'API KEY',
  content: {
    // ------ REQUIRED ------
    body: 'SMS CONTENT',
    mobileNumbers: ['MOBILE'], //  '09153752127'

    // ------ OPTIONAL ------
    scheduler: 'YYYY-MM-HH HH:MM' | DATE
  },

  // ------ OPTIONAL ------
  serviceName: 'SERVICE NAME'
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.sendSMS(sendSMSData)
  .then(function (result) {
    console.log('function: sendSMS ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error sendSMS ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>

## دریافت وضعیت پیامک ارسال شده
به منظور تعیین وضعیت پیامک ارسال شده می ­توان از سرویس ذیل استفاده کرد.
ورودی این سرویس، شناسه پیامک ارسال شده ­ای است که در سرویس ارسال پیامک دریافت شده است.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
        var output = new ResultSrv<SmsStatusSrv>();
        var smsStatusVo = SmsStatusVo.ConcreteBuilder
                            .SetToken("{Put your Token}")
                            .SetContent("{Put your Content}")
                            .Build();
        notificationService.GetSmsStatus(smsStatusVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
   BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();

        try {
            SmsStatusVo smsStatusVo = new SmsStatusVo.Builder(baseInfoVo)
                    .setNotificationMessageId({messageId})
                    .build();
            podNotification.getSmsStatus(smsStatusVo, new OnGetResponseListener<SmsStatuseSrv>() {
                @Override
                public void onResponse(ResultVo<SmsStatuseSrv> result) {
                    Map.Entry<Long, MessageStatusVO> entry = result.getResult().getResult().getContent().getMessage().entrySet().iterator().next();
                    Long l1 = entry.getKey();
                    MessageStatusVO value = entry.getValue();
                    System.out.println(value.getMessageStatus());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "content" => [
            ## ======== Required Parameters ==============
            "notificationMessageId" => "{put Notification Message Id}"
        ]
    ];
    try {
        $result = $NotificationService->getSMSStatus($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let getSMSStatusData = {
      // ------ REQUIRED ------
      scApiKey: 'API KEY',
      messageId: 'MESSAGE ID'

      // ------ OPTIONAL ------
      // scVoucherHash: ['HASH#1', 'HASH#2']

      // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
      // token: 'TOKEN'
      // tokenIssuer: 0 | 1
    };

podNotification.getSMSStatusData(getSMSStatusData)
  .then(function (result) {
    console.log('function: getSMSStatusData ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error getSMSStatusData ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>


## دریافت لیست پیام های ارسالی
یک کاربر می­تواند با استفاده از این سرویس، لیست پیامک­هایی که ارسال کرده است را مشاهده کند.
با استفاده از این سرویس می­توان پیامک اعتبارسنجی به یک فرد ارسال کرد. این سرویس امکان ارسال پیام به شماره­ های بین ­المللی را نیز دارد.
با استفاده از این سرویس، می توان از وضعیت پیامک ارسال شده اطلاع پیدا کرد. برای استفاده از این سرویس باید شناسه پیام ارسال شده به عنوان ورودی داده شود.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();

        var output = new ResultSrv<List<SmsListSrv>>();
        var smsListVo = SmsListVo.ConcreteBuilder
                            .SetToken({Put your Token})
                            //.SetOffset({Put your Offset})
                            //.SetSize({Put your Size})
                            //.SetFilter({Put your Filter})
                            //.SetFilterValue("{Put your FilterValue}")
                            //.SetOrder({Put your Order})
                            //.SetOrderBy({Put your OrderBy})
                            .Build();
        notificationService.GetSmsList(smsListVo, response => Listener.GetResult(response, out output));            
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
   BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();

        try {
            SmsListVo smsListVo = new SmsListVo.Builder(baseInfoVo)
                    .build();

            podNotification.getSmsList(smsListVo, new OnGetResponseListener<SmsListSrv>() {
                @Override
                public void onResponse(ResultVo<SmsListSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().get(0).getContent());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php

```

# [Nodejs](#tab/nodejs)

``` javascript
let getSMSListData = {
  // ------ REQUIRED ------
  scApiKey: 'API KEY'

  // ------ OPTIONAL ------
  // offset: 0,
  // size: 0
  // orderBy: 'COLUMN',
  // order: 'acs | desc',
  // filter: 'FILTER',
  // filterValue: 'FILTER VALUE'
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.getSMSList(getSMSListData)
  .then(function (result) {
    console.log('function: getSMSList ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error getSMSList ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>


## ارسال ایمیل
با استفاده از این سرویس می­توانید یک ایمیل را به یک یا گروهی از افراد ارسال کنید. این سرویس قابلیت اضافه کردن فایل ضمیمه، cc و bcc نیز می­باشد.
به منظور ارسال ایمیل همراه با فایل ضمیمه، لازم است ابتدا فایل­های مورد نظر در پاد اسپیس به صورت public ذخیره شود. سپس با استفاده از hash (­های) مربوطه می­توانید ایمیل خود را همراه با فایل(های) ضمیمه ارسال کنید.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();

        var output = new ResultSrv<SmsSrv>();
        var emailVo = EmailVo.ConcreteBuilder
                            .SetToken(apiToken)
                            .SetContent(EmailContentVo.ConcreteBuilder
                                .SetTo({Put your Emails})
                                //.SetReplyAddress("{Put your ReplyAddress}")
                                //.SetBody("{Put your Body}")
                                //.SetSubject("{Put your Body}")
                                //.SetCc({Put your Cc})
                                //.SetBcc({Put your Bcc})
                                //.SetFileHashes({Put your FileHashes})
                                //.SetScheduler({Put your Schedular})
                                .Build())
                            //.SetServiceName("{Put your ServiceName}")
                            .Build();
        notificationService.SendEmail(emailVo, response => Listener.GetResult(response, out output));            
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
 BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();
        String[] to = new String[]{{to}};

        EmailContentVo emailContentVo = null;
        try {
            emailContentVo = new EmailContentVo.Builder(baseInfoVo)
                    .setTo(to)
                    .build();
        } catch (PodException e) {
        }
        BodySendAEmail bodySendAEmail = null;

        try {
            bodySendAEmail = new BodySendAEmail.Builder()
                    .setContent(emailContentVo)
                    .build();
        } catch (com.fanap.podBaseService.exception.PodException ex) {
            ex.printStackTrace();
        }


        try {
            EmailVo emailVo = new EmailVo.Builder(baseInfoVo)
                    .setBodySendAEmail(bodySendAEmail)
                    .build();
            podNotification.sendEmail(emailVo, new OnGetResponseListener<EmailSrv>() {
                @Override
                public void onResponse(ResultVo<EmailSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().getMessageId());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "content" => [
            ## ======== Required Parameters ==============
            "to" => ["emailAddress1", "emailAddress2"],
            ## ======== Optional Parameters  =============
            "subject" => "email subject",
            "content" => "email content",
            "replyAddress" => "put reply address",
            "cc" => "put email address",
//            "bcc" => "put email address",
//            "fileHashes" => "",
//            "scheduler" => "yyyy-MM-dd HH:mm"
        ],
        # ============================ Optional Parameters  ==================================
//        "sreviceName" => ""
    ];
    try {
        $result = $NotificationService->sendEmail($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let sendEmailData = {
  // ------ REQUIRED ------
  scApiKey: 'API KEY',
  content: {
    // ------ REQUIRED ------
    to: ['mail@gmail.com'],
    body: 'EMAIL CONTENT'

    // ------ OPTIONAL ------
    // subject: 'SUBJECT',
    // replyAddress: 'REPLAY ADDRESS',
    // cc: ['e.shekari@gmail.com']
    // bcc: ['e.shekari@gmail.com']
    // fileHashes: ['HASH1']
    // scheduler: 'DATE'
  }

  // ------ OPTIONAL ------
  // serviceName: 'SERVICE NAME'
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.sendEmail(sendEmailData)
  .then(function (result) {
    console.log('function: sendEmail ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error sendEmail ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>

## دریافت لیست ایمیل های ارسالی
شما می­ تواند با استفاده از این سرویس، لیست ایمیل­هایی که ارسال کرده است را مشاهده کند.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
        var output = new ResultSrv<List<EmailListSrv>>();
        var emailListVo = EmailListVo.ConcreteBuilder
                            .SetToken(apiToken)
                            //.SetOffset({Put your Offset})
                            //.SetSize({Put your Size})
                            //.SetFilter({Put your FilterField})
                            //.SetFilterValue("{Put your FilterValue}")
                            //.SetOrder(OrderType.desc)
                            //.SetOrderBy({Put your OrderBy field})
                            .Build();
        notificationService.GetEmailList(emailListVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
 BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();

        try {
            EmailListVo emailListVo = new EmailListVo.Builder(baseInfoVo)
                    .build();
            podNotification.getEmailList(emailListVo, new OnGetResponseListener<EmailListSrv>() {
                @Override
                public void onResponse(ResultVo<EmailListSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().get(0).getContent());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        ## ============================ Optional Parameters  ==================================
//        "offset" => 0,
//        "size" => "put output size",
//        "orderBy" => "{put id or sendDate}",
//        "order" => "{asc or desc}",
//        "filter" => "{put message or subject or receiver}",
//        "filterValue" => "{put search value}",
    ];
    try {
        $result = $NotificationService->getEmailList($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {
        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let getEmailListData = {
  // ------ REQUIRED ------
  scApiKey: 'API KEY',

  // ------ OPTIONAL ------
  // offset: 0,
  // size: 0
  // orderBy: 'COLUMN,
  // order: 'acs | desc',
  // filter: 'FILTER',
  // filterValue: 'FILTER VALUE'
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.getEmailList(getEmailListData)
  .then(function (result) {
    console.log('function: getEmailList ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error getEmailList ============>', error);
  });
```

<div class="tab-end">
</div>

#### دریافت ایمیل های ارسال شده با استفاده از شناسه
با استفاده از این سرویس می­توانید اطلاعات ایمیل ارسال شده را با استفاده از شناسه آن دریافت نمود

<div class="box-end">
</div>

## ارسال push notification

#### ارسال push notification از طریق peer
با استفاده از این سرویس، امکان ارسال push notification برای کاربران فراهم شده است. ارسال push notification با استفاده از application id (push notification برای تمام دستگاه­هایی که از این application استفاده می­کنند ارسال می­شود) و یا peer Id کاربران انجام می­شود


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
        var output = new ResultSrv<SmsSrv>();
        var pushNotificationByPeerIdVo= PushNotificationByPeerIdVo.ConcreteBuilder
                            .SetToken({Put your Token})
                            .SetContent(PushNotificationByPeerIdContentVo.ConcreteBuilder
                                .SetPeerId({Put your PeerId})
                                //.SetTitle("{Put your Title}")
                                //.SetText("{Put your Text}")
                                //.SetExtraData("{Put your ExtraData}")
                                //.SetScheduler({Put your Scheduler})
                                .Build())
                            .Build();
        notificationService.SendPushNotification(pushNotificationByPeerIdVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();
        Long[] recivers = new Long[]{{recivers}};
        PushNotificationByPeerIdContentVo pushNotificationByPeerIdContentVo = null;
        try {
            pushNotificationByPeerIdContentVo = new PushNotificationByPeerIdContentVo.Builder(baseInfoVo)
                    .setReceivers(recivers)
                    .setTitle({title})
                    .setText({text})
                    .build();
        } catch (PodException e) {

        }

        BodySendPushNotificationByPeerIdVo bodySendPushNotificationByPeerIdVo = null;
        try {
            bodySendPushNotificationByPeerIdVo = new BodySendPushNotificationByPeerIdVo.Builder()
                    .setContent(pushNotificationByPeerIdContentVo)
                    .build();
        } catch (PodException e) {
            e.printStackTrace();
        }

        try {
            PushNotificationByPeerIdVo pushNotificationByPeerIdVo = new PushNotificationByPeerIdVo.Builder(baseInfoVo)
                    .setBodySendPushNotificationByPeerIdVo(bodySendPushNotificationByPeerIdVo)
                    .build();
            podNotification.SendPushNotificationByPeerId(pushNotificationByPeerIdVo, new OnGetResponseListener<SmsSrv>() {
                @Override
                public void onResponse(ResultVo<SmsSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().getMessageId());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "content" => [
            ## ======== Required Parameters ==============
            "receivers" => ["peerId1","peerId2"],
            ## ======== Optional Parameters  =============
            "title" => "test push notification",
            "text" => "test",
            "extra" => "test",
//            "scheduler" => "yyyy-MM-dd HH:mm",
        ]
    ];
    try {
        $result = $NotificationService->pushNotificationByPeerId($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {
        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let pushNotificationByPeerIdData = {
  // ------ REQUIRED ------
  apiToken: 'API TOKEN',
  content: {
    // ------ REQUIRED ------
    receivers: [3964535]

    // ------ OPTIONAL ------
    // text: 'TEXT',
    // title: 'TITLE',
    // extra: 'EXTRA'
    // scheduler: 'DATE'
  }
};

podNotification.pushNotificationByPeerId(pushNotificationByPeerIdData)
  .then(function (result) {
    console.log('function: pushNotificationByPeerId ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error pushNotificationByPeerId ============>', error);
  });
```

<div class="tab-end">
</div>

#### ارسال push notification برای application خاص
با استفاده از این سرویس می­توان برای افرادی که در حال استفاده از یک application خاص هستند نوتیفیکیشن ارسال کرد.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
        var output = new ResultSrv<SmsSrv>();
        var pushNotificationByAppIdVo = PushNotificationByAppIdVo.ConcreteBuilder
                            .SetToken({Put your Token})
                            .SetContent(PushNotificationByAppIdContentVo.ConcreteBuilder
                                .SetAppId("{Put your AppId}")
                                //.SetTitle("{Put your Title}")
                                //.SetText("{Put your Text}")
                                //.SetScheduler({Put your Scheduler})
                                .Build())
                            .Build();
        notificationService.SendPushNotification(pushNotificationByAppIdVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
   BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();
        Long[] recivers = new Long[]{{recivers}};
        PushNotificationByAppIdContentVo pushNotificationByAppIdContentVo = null;
        try {
            pushNotificationByAppIdContentVo = new PushNotificationByAppIdContentVo.Builder(baseInfoVo)
                    .setAppId({appId})
                    .build();
        } catch (PodException e) {

        }

        BodySendPushNotificationByAppIdVo bodySendPushNotificationByAppIdVo = null;
        try {
            bodySendPushNotificationByAppIdVo = new BodySendPushNotificationByAppIdVo.Builder()
                    .setContent(pushNotificationByAppIdContentVo)
                    .build();
        } catch (PodException e) {
            e.printStackTrace();
        }

        try {
            PushNotificationByAppIdVo pushNotificationByAppIdVo = new PushNotificationByAppIdVo.Builder(baseInfoVo)
                    .setBodySendPushNotificationByAppIdVo(bodySendPushNotificationByAppIdVo)
                    .build();
            podNotification.SendPushNotificationByAppId(pushNotificationByAppIdVo, new OnGetResponseListener<SmsSrv>() {
                @Override
                public void onResponse(ResultVo<SmsSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().getMessageId());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "content" => [
            ## ======== Required Parameters ==============
            "appId" => "put application id",
            ## ======== Optional Parameters  =============
            "title" => "test push notification",
            "text" => "test",
//            "scheduler" => "yyyy-MM-dd HH:mm",
        ]
    ];
    try {
        $result = $NotificationService->pushNotificationByAppId($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let pushNotificationByAppIdData = {
  // ------ REQUIRED ------
  apiToken: 'API TOKEN',
  content: {
    // ------ REQUIRED ------
    appId: 'appId'

    // ------ OPTIONAL ------
    // text: 'TEXT',
    // title: 'TITLE',
    // extra: 'EXTRA'
    // scheduler: 'DATE'
  }
};

podNotification.pushNotificationByAppId(pushNotificationByAppIdData)
  .then(function (result) {
    console.log('function: pushNotificationByAppId ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error pushNotificationByAppId ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>


## دریافت لیست push notification
یک کاربر می­تواند با استفاده از این سرویس، لیست push notification هایی که ارسال کرده است را مشاهده کند.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
            var output = new ResultSrv<List<PushNotificationListSrv>>();
            var pushNotificationListVo = PushNotificationListVo.ConcreteBuilder
                                .SetToken({Put your Token})
                                //.SetOffset({Put your Offset})
                                //.SetSize({Put your Size})
                                //.SetFilter({Put your Filter field})
                                //.SetFilterValue("{Put your Filter Value}")
                                //.SetOrder({Put your Order Type})
                                //.SetOrderBy({Put your OrderBy field})
                                .Build();
            notificationService.GetPushNotificationList(pushNotificationListVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
   BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({scApiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();

        try {
            PushNotificationListVo pushNotificationListVo = new PushNotificationListVo.Builder(baseInfoVo)
                    .build();

            podNotification.getPushNotificationList(pushNotificationListVo, new OnGetResponseListener<PushNotificationListSrv>() {
                @Override
                public void onResponse(ResultVo<PushNotificationListSrv> result) {
                    System.out.println(result.getResult().getResult().getCount());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "messageId" => "put message id"
        "statusType" => "{put `all` or `received` or `seen`}" 
    ];
    try {
        $result = $NotificationService->getPushNotificationStatus($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let getPushNotificationListData = {
  // ------ REQUIRED ------
  scApiKey: '72beef7d00fb49e9b2e2ac3907838061',

  // ------ OPTIONAL ------
  offset: 0,
  size: 2
  // orderBy: 'COLUMN',
  // order: 'acs | desc',
  // filter: 'FILTER',
  // filterValue: 'FILTER VALUE'
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.getPushNotificationList(getPushNotificationListData)
  .then(function (result) {
    console.log('function: getPushNotificationList ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error getPushNotificationList ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>

## دریافت وضعیت push notification
با استفاده از این سرویس می­توان وضعیت نوتیفیکشن­(های) ارسالی را دریافت کرد. بدین منظور باید از شناسه پیامی که در هنگام ارسال نوتیفیکیشن دریافت شده است استفاده کرد.
پیام­ها را می­توان در سه حالت “همه وضعیت­ها”، “دریافت شده”، “دیده شده” دریافت کرد.


<div class="tab-start">
</div>

# [C#](#tab/csharp)

``` csharp
try
    {
        var internalServiceCallVo = InternalServiceCallVo.ConcreteBuilder
            .SetToken("{Put your ApiToken}")
            //.SetScVoucherHash({Put your VoucherHash})
            //.SetScApiKey("{Put your ApiKey}")
            .Build();
            
        var output = new ResultSrv<SmsStatusSrv>();
        var pushNotificationStatusVo = PushNotificationStatusVo.ConcreteBuilder
                            .SetToken({Put your Token})
                            .SetMessageId("{Put your MessageId}")
                            .SetStatusType({Put your StatusType})
                            .Build();
        notificationService.GetPushNotificationStatus(pushNotificationStatusVo, response => Listener.GetResult(response, out output));
    }
catch (PodException podException)
    {
        Console.WriteLine($"-- {podException.Code}-an error has occured : 
                              {Environment.NewLine{podException.Message}");
        throw;
    }
catch (Exception exception)
    {
        Console.WriteLine(exception.Message);
        throw;
    }
```

# [Java](#tab/java)

``` java
BaseInfoVo baseInfoVo = new BaseInfoVo.Builder()
                .setToken({token})
                .setScApiKey({apiKey})
                .setServerType(Enum_Server_type.SANDBOX)
                .setToken_issuer(1)
                .build();

        PodNotification podNotification = new PodNotification();

        try {
            PushNotificationStatusVo pushNotificationStatusVo = new PushNotificationStatusVo.Builder(baseInfoVo)
                    .setStatusType({statuseType})
                    .setMessageId({messageId})
                    .build();
            podNotification.getPushNotificationStatus(pushNotificationStatusVo, new OnGetResponseListener<SmsStatuseSrv>() {
                @Override
                public void onResponse(ResultVo<SmsStatuseSrv> result) {
                    System.out.println(result.getResult().getResult().getContent().getMessageId());
                }

                @Override
                public void onFailed(PodException e) {
                    System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

                }
            });
        } catch (PodException e) {
            System.out.println("code : " + e.getCode() + "\nmessage : " + e.getMessage());

        }
```

# [PHP](#tab/php)

``` php
$params = [
        ## ============================ Required Parameters  ==================================
        "token" => "{API_TOKEN or ACCESS_TOKEN}",
        "messageId" => "put message id"
        "statusType" => "{put `all` or `received` or `seen`}" 
    ];
    try {
        $result = $NotificationService->getPushNotificationStatus($params);
        print_r($result);
    }
    catch (ValidationException $e) {
        print_r($e->getErrorsAsArray());
    }
    catch (PodException $e) {

        print_r(
            $e->GetResult()
        );
    }
```

# [Nodejs](#tab/nodejs)

``` javascript
let getPushNotificationStatusData = {
  // ------ REQUIRED ------
  scApiKey: 'API KEY',
  messageId: 'MESSAGE ID',
  statusType: 'all | received |seen'

  // ------ OPTIONAL ------
  // scVoucherHash: ['HASH#1', 'HASH#2']

  // ------ OPTIONAL (JUST CHANGE THE CURRENT REQUEST) ------
  // token: 'TOKEN'
  // tokenIssuer: 0 | 1
};

podNotification.getPushNotificationStatus(getPushNotificationStatusData)
  .then(function (result) {
    console.log('function: pushNotificationStatusAll ============>', result, '\n');
  })
  .catch(function (error) {
    console.log('error pushNotificationStatusAll ============>', error);
  });
```

<div class="tab-end">
</div>

<div class="box-end">
</div>

## لینک‌های مرتبط

- [مقدمه](/documents/introduction/)
- [مفاهیم](/documents/concepts/)
- [استاندارد و خطاها](/documents/errors/)
- [شروع به کار](/documents/get-started/)
- [پکیج‌ها](/documents/packages/)

<div class="box-end">
</div>

